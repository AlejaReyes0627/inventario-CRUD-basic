server:
  port: 8088
  servlet:
    context-path: /inventario

spring:
  datasource:
    url: jdbc:postgresql://aws-0-us-east-2.pooler.supabase.com:6543/postgres?sslmode=require
    username: postgres.dwiadfpoekrtxvdmcvhy
    password: Y29udHJhc2XDsWE=
    driver-class-name: org.postgresql.Driver
  
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true